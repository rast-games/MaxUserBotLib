# MaxApi

`MaxApi` is the asynchronous high-level client. It owns the selected backend stack and exposes operations for messages, chats, reactions, polls, groups, contacts, profile, folders, sessions, presence, and two-factor authentication.

::: pyromax.core.client.MaxApi
    options:
      members: true
      inherited_members: true
      show_root_heading: false
